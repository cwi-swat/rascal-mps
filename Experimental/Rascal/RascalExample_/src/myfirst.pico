begin
	declare 
		n : natural, 
		f : natural,
		i : natural,
		s : string,
		b : string;
		
		s := "a";
		
		if not n then 
			n := n + 1;
			f := f + 1
		else
			n := n
		fi;
		
		b := "";
		for i := 1; not (i == 10); i := i + 1 
		do
			b := b || "1" 
		od;
		
		if n and f then
			f := f + 1
		else
		fi;
		
		while n do 
			s := s || "a"; 
			n := n - 1
		od
end