begin
	declare
		a : natural,
		b : natural;
		
		% the final part of the for loop must be a statement to determine how to increment the loop %
		% since a is an expression instead of a statement the parsing fails %
		for  a := 1 ; not(a == 5); a do
			b := b + 1
		od	
end